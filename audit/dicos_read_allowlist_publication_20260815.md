# DiCOS read-allowlist publication — 2026-08-15

Commit `c061d37` (`fix(dicos): enforce two-path read scope`) was published to
`origin/main` after a fresh fetch proved the remote had no unintegrated commit
(`origin/main...HEAD` was `0 1`). Push result:
`9d2d8d1..c061d37 main -> main`.

The publication includes the exact two-entry DiCOS read rule, its client and
operator-tool enforcement, offline regression tests, the full audit twin, and
the existing watcher's verified S2-response epoch-3/4 evidence. No test data
was used and no live job was launched, restarted, or interrupted.

**PHYSICS VALIDATION NOT ESTABLISHED.**
