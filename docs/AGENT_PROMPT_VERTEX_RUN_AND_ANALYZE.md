# Compatibility pointer

This older filename is retained because prior chats and notes link to it.

Use the complete current prompt:

```text
docs/AGENT_PROMPT_CONTINUE_ANY_BACKEND_20260728.md
```

That prompt includes Vertex AI and non-Vertex execution, exact artifact
identities, detector/data/model contracts, website publication, current
epoch-4 results, and the nonblocking QA policy.
